<%= form_with(model: training_session, local: true) do |form| %>
  <% if training_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(training_session.errors.count, "error") %> prohibited this training_session from being saved:</h2>

      <ul>
        <% training_session.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :planned_distance %>
    <%= form.text_field :planned_distance %>
  </div>

  <div class="field">
    <%= form.label :actual_distance %>
    <%= form.text_field :actual_distance %>
  </div>

  <div class="field">
    <%= form.label :instructions %>
    <%= form.text_area :instructions %>
  </div>

  <div class="field">
    <%= form.label :athletes_notes %>
    <%= form.text_area :athletes_notes %>
  </div>

  <div class="field">
    <%= form.label :coaches_notes %>
    <%= form.text_area :coaches_notes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
